name: VeuDemo
options:
  bundleIdPrefix: com.veu.protocol
  deploymentTarget:
    iOS: "26.0"
  xcodeVersion: "26.0"
  createIntermediateGroups: true

packages:
  VeuApp:
    path: ../../packages/veu-app

targets:
  VeuDemo:
    type: application
    platform: iOS
    sources:
      - Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.veu.protocol.demo
        MARKETING_VERSION: "0.6.0"
        CURRENT_PROJECT_VERSION: 1
        INFOPLIST_FILE: Sources/Info.plist
        SWIFT_VERSION: "5.9"
        GENERATE_INFOPLIST_FILE: NO
        DEVELOPMENT_TEAM: ""
      configs:
        Debug:
          CODE_SIGN_ENTITLEMENTS: Sources/VeuDemo.dev.entitlements
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG DEV_MODE
        Release:
          CODE_SIGN_ENTITLEMENTS: Sources/VeuDemo.entitlements
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: RELEASE
    dependencies:
      - package: VeuApp
      - sdk: MultipeerConnectivity.framework
      - sdk: NearbyInteraction.framework
    resources:
      - path: Assets.xcassets
